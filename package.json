{
  "name": "profile",
  "version": "1.0.0",
  "description": "Personal PWA profile page — Vite vanilla TypeScript",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^7.3.1"
  }
}
